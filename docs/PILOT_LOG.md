# Pilot Log — Pilot v1 (Dogfooding)

> Запрошено владельцем проекта явно (2026-08-04): "через две недели этот журнал будет ценнее сотни придуманных улучшений". Ведётся по факту, день за днём, начиная с деплоя. Не архитектурный документ — сырой протокол: что сделали, что сломалось, что мешало, что осталось руками. Решения по доработкам принимаются только после прохождения полного цикла партии (`docs/MASTER_BACKLOG.md`, раздел «Pilot v1»), не по ходу — этот файл только фиксирует факты, не проектирует исправления.
>
> **Разделение с `docs/PILOT_BUGS.md` (уточнено 2026-08-04):** этот файл — вехи и общий ход дня (деплой прошёл, компания создана, партия перешла в статус X). Конкретные проблемы, найденные при работе с системой — метка критичности, что делали/ожидали/произошло, статус исправления — заводятся **только** в `docs/PILOT_BUGS.md`, не здесь и не дублируются. Если запись в этом журнале — по сути баг, перенести её в `docs/PILOT_BUGS.md`.

## Как заполнять

Одна запись на каждое значимое событие (не обязательно раз в день). Формат:

```
### <дата> — <что происходило>

**Сделали:** ...
**Проблема:** ... (если была)
**Исправлено:** ... / **Осталось:** ...
**Что мешало / заняло слишком много времени:** ...
```

Пусто — значит, ничего значимого не произошло, запись не нужна. Не придумывать записи ради заполнения.

## Цели Pilot v1 (напоминание, `docs/MASTER_BACKLOG.md`)

1. Деплой на Railway.
2. Первая настоящая компания (не демо).
3. 2–3 настоящие модели, 2 настоящих цеха.
4. Первая настоящая производственная партия — полный цикл, до денег и логистики.

## Журнал

### 2026-09-21 — Первый реальный production-деплой + ручное применение миграций

**Сделали:**
- Push ветки `claude/garmentos-foundation-architecture-ahvh6b` (коммит `fe043dd` — `feat(production): production order to specification flow`) автоматически задеплоил production API и web на Railway (проект `disciplined-perfection`, окружение `production`): `GarmentOS` (API, https://garmentos-production.up.railway.app), `web` (https://web-production-c6b5c.up.railway.app). Auto-deploy сработал как побочный эффект push, не был инициирован отдельной командой — на будущее учитывать, что push в эту ветку = деплой в production.
- `preDeployCommand` у API-сервиса пуст — миграции не применяются автоматически при деплое.
- Владелец проекта вручную (Windows, PowerShell + Railway CLI + локальный PostgreSQL client) через Railway TCP tunnel создал backup production БД: `garmentos-production-20260921-185121.dump` (~171 KB), проверен через `pg_restore --list`, архив читается корректно. Более ранняя попытка (`garmentos-production-20260921-184839.dump`, 0 байт) — невалидна, мусор.
- После backup владелец вручную применил через тот же tunnel миграции `0030_sticky_jimmy_woo`, `0031_smooth_human_robot`, `0032_production_master_stage4_status_specification_link`, `0033_seed_rollback_permission_and_presets` — до этого production находился на `0029_production_master_stage3` (на 4 миграции позади HEAD, не только на 2, как предполагалось изначально; все 4 миграции аддитивные — новые enum-значения, новые таблицы, новые колонки/индексы/permission, ни одного DROP/DELETE/data loss).
- Соответствие подтверждено сверкой SHA-256 локальных файлов миграций с production migration journal: `0030`→journal id 31, `0031`→32, `0032`→33, `0033`→34.
- После миграций: `GET /v1/health` → `200`, production web открывается, авторизация/интерфейс доступны.

**Проблема:** auto-deploy на push не был предусмотрен явно в процессе — код оказался в production раньше, чем схема БД была приведена в соответствие (окно рассинхрона: код `fe043dd` живой с ~07:00 UTC, миграции применены позже вручную). За это время новых production-эндпоинтов, зависящих от 0030–0033, никто не вызывал (подтверждено по deploy-логам — 0 HTTP-запросов за этот период), поэтому фактического сбоя не произошло.

**Исправлено:** миграции применены, схема соответствует `fe043dd`, backup существует.
**Осталось:** нет описанного в `docs/LAUNCH_DAY.md`/`docs/INFRASTRUCTURE.md` runbook-шага «применить миграции перед первым использованием после auto-deploy» — стоит добавить отдельно, чтобы не полагаться на ручную память. Preview-проект `garmentos-preview-09` (отдельная БД, отдельный volume, не задет) — трогать не будем до отдельного решения.
**Что мешало / заняло слишком много времени:** отсутствие read-only доступа к production Postgres из агентской сессии (Railway MCP не отдаёт значения переменных, egress-прокси блокирует прямой сетевой доступ) — все проверки схемы и сами миграции пришлось выполнять владельцу вручную через браузерную SQL-консоль Railway и через Railway CLI/tunnel с личного компьютера.

## Отложенные находки (не чиним сейчас, чтобы не потерять)

### 2026-09-22 — откат placed → draft ломает повторное подтверждение заказа (costSnapshot не сбрасывается)

Найдено попутно при точечной UI-правке кнопки отката статуса на паспорте партии (см. коммит с исправлением видимости кнопки для случая `received` без факта приёмки). Не является багом этой правки — обнаружено при проверке, насколько условие видимости кнопки может/должно точно соответствовать backend-guard'у.

**Механизм:** `assertCanRollbackStatus` (`packages/domain/contract-manufacturing/src/domain/production-order.ts`) технически разрешает откат `placed → draft` (это не последний статус в цепочке, ОТК/факта приёмки на этой стадии ещё не бывает). Но `rollbackProductionOrderStatus` только переводит статус назад — `costSnapshot`, зафиксированный при первом подтверждении заказа, не сбрасывается. Если после такого отката заказ подтвердить повторно ("Подтвердить" на паспорте черновика), `confirmProductionOrder` пройдёт, но `captureProductionOrderCostSnapshot` упадёт с `PRODUCTION_ORDER_COST_SNAPSHOT_ALREADY_SET` (409) — заказ при этом уже успеет перейти в `placed`, а пользователь увидит непонятную ошибку вместо результата.

**Почему не чиним сейчас:** UI-кнопку отката для статуса `placed` намеренно не показываем (сохранили существующее ограничение) — то есть через интерфейс это состояние сегодня не воспроизвести. Риск есть только при прямом вызове `POST /production-orders/:id/rollback-status` на заказе в `placed` (API это позволяет), что не относится к текущему сценарию использования системы.

**Что нужно сделать, когда дойдут руки:** либо запретить `placed → draft` явно в `assertCanRollbackStatus` (сузить guard), либо научить rollback сбрасывать `costSnapshot` при возврате в `draft`, либо `captureProductionOrderCostSnapshot` — разрешать перезапись после отката (аккуратно, это разрушит гарантию неизменности снимка). Решение — архитектурное, не точечное, поэтому не принимается по ходу мелкой UI-правки.

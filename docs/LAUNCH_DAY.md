# День запуска GarmentOS

> Запрошено владельцем проекта явно (2026-08-04): не начинать Railway «как получится», а выделить отдельный день под запуск с явным чек-листом. Каждый пункт выполняется руками владельца проекта (нет доступа к railway.app из песочницы, см. `infra/RAILWAY_DEPLOY.md`) — этот файл только фиксирует порядок и критерий «готово», не может быть выполнен автоматически.
>
> **С момента, когда отмечен последний пункт, GarmentOS перестаёт быть проектом разработки и становится рабочим инструментом.** Дальше — эксплуатация (`docs/MASTER_BACKLOG.md`, раздел 0.5), а не разработка.

## Чек-лист

- [ ] Railway развёрнут (`infra/RAILWAY_DEPLOY.md`, шаги 1-2).
- [ ] Домен работает (`Settings → Networking → Generate Domain` для `api` и `web`).
- [ ] HTTPS работает (автоматически на `*.up.railway.app`).
- [ ] Вход работает (`POST /auth/login` реальным пользователем — `infra/PRODUCTION_CHECKLIST.md`, smoke test п.2).
- [ ] PDF скачивается (`GET /documents/:id/file` — smoke test п.8, включая проверку после передеплоя, п.10 — Volume подключён).
- [ ] Создана реальная компания (`bootstrap-company.script.js`, `infra/RAILWAY_DEPLOY.md`, шаг 7).
- [ ] Созданы реальные модели (2-3, `docs/MASTER_BACKLOG.md`, раздел 0.5, цель 3).
- [ ] Созданы реальные цеха (2, с `contractNumber` — обязателен для подтверждения заказа).
- [ ] Создана первая партия (заказ пошива на реальную модель/реальный цех).
- [ ] Сгенерирована первая настоящая спецификация (`POST /production-orders/:id/generate-specification`).
- [ ] PDF отправлен бухгалтеру цеха.
- [ ] Первая запись появилась в `docs/PILOT_LOG.md`.

## После отметки последнего пункта

1. Дальнейшие решения принимаются только на основании реальной эксплуатации — не по предположениям, не по идеям, не по архитектурным соображениям (`CLAUDE.md`, правило 10).
2. Новые проблемы фиксируются исключительно в `docs/PILOT_BUGS.md`, метки 🔴/🟡/🟢, исправление сверху вниз.
3. Новый функционал не добавляется, пока не проявится необходимость в реальной работе (Freeze архитектуры, `docs/MASTER_BACKLOG.md`, раздел 0.5).
4. **После завершения первой реальной партии** (критерий — `docs/MASTER_BACKLOG.md`, раздел 0.5, «Критерий завершения»: полный цикл без Excel) — полноценный разбор Pilot v1 по `docs/PILOT_BUGS.md` и `docs/PILOT_LOG.md`, и только тогда формируется Roadmap v2. Не раньше.

# Roadmap разработки GarmentOS

> Цели каждой фазы прослеживаются к целям 1/3/5 лет из [`PROJECT_VISION.md`](../PROJECT_VISION.md). Фазы — не фиксированные календарные кварталы, а последовательность с exit-критериями: следующая фаза не начинается, пока не закрыты критерии предыдущей.

## Фаза 0 — Фундамент — **ЗАКРЫТА** (2026-07-24)

**Цель**: заложить архитектуру, которая не потребует переписывания при росте.

- [x] Видение проекта (`PROJECT_VISION.md`).
- [x] Архитектура системы (`docs/ARCHITECTURE.md`).
- [x] Технологический стек (`docs/TECH_STACK.md`).
- [x] Структура репозитория (`docs/REPOSITORY_STRUCTURE.md`).
- [x] Структура базы данных (`docs/DATABASE_SCHEMA.md`).
- [x] Принципы разработки и стандарты качества.
- [x] Инфраструктурная стратегия (`docs/INFRASTRUCTURE.md`) — cloud-agnostic, Start Lean Scale Smart.
- [x] Мультитенантность — окончательно: единая БД, изоляция по `company_id` (`docs/ARCHITECTURE.md` п.6).
- [x] Первый маркетплейс — окончательно: Wildberries, через `MarketplaceConnector` (`docs/ARCHITECTURE.md` п.5.1).
- [x] Local-First и AI-First зафиксированы как архитектурные принципы (`docs/PRINCIPLES.md`, пп.14-15; `docs/ARCHITECTURE.md`, пп.11-12).
- [x] Финальная самопроверка архитектуры (`docs/ARCHITECTURE_SELF_REVIEW.md`) — блокирующих вопросов не осталось.

**Exit-критерий выполнен**: владелец проекта подтвердил архитектуру, все открытые вопросы закрыты явными решениями (зафиксированы в документах выше). Реализация бизнес-логики разрешена начиная с Фазы 1.

## Фаза 1 — MVP: сквозной цикл на одном канале продаж

**Цель**: рабочий end-to-end поток для одной пилотной компании — от закупки до продажи на одном маркетплейсе.

Состав (соответствует границам MVP в `PROJECT_VISION.md`, п.6):

1. **Identity & Access** — компании, пользователи, роли (director/accountant/procurement/technologist/marketplace_manager/warehouse_keeper/contract_manufacturing_manager), базовый RBAC.
2. **Catalog** — модели, SKU (размер × цвет).
3. **Materials & Procurement** — материалы, поставщики, простые заказы поставщикам.
4. **BOM** — спецификации расхода, версионирование.
5. **Contract Manufacturing** (не «Production» — см. `docs/DATABASE_SCHEMA.md` п.0) — цеха-подрядчики (`workshops`), заказы пошива без внутренней стадийности (крой/пошив/ОТК — не наша операционка).
6. **Warehouse & Inventory** — приёмка, отгрузка, остатки по SKU, инвентаризация, включая склад-локацию у цеха для давальческого сырья.
7. **Sales & Orders** — ручное создание заказов + интеграция с Wildberries (остатки, цены, заказы).
8. **Honest Sign** — MVP-уровень: учёт кодов, базовые статусы (`issued → applied → sold`).
9. **Finance** — базовая себестоимость (материалы + услуги цеха), без полного P&L.
10. **Inbox** (добавлен 2026-07-24 по итогам `docs/USER_JOURNEY_AUDIT.md`) — Telegram-канал + загрузка файлов, AI-классификация входящих сообщений, one-tap подтверждение. См. `docs/INBOX_ARCHITECTURE.md`.
11. **AI Production Assistant, Document Engine, Telegram Integration** (перенесены из Фазы 2 в Фазу 1 решением владельца проекта 2026-07-26 — см. «Пересмотр стратегии» ниже) — первый сквозной вертикальный сценарий («Отшей Двойку, Петроль 1000 шт» → расчёт материалов → PDF-спецификация → отправка в цех через Telegram → статус выполнения), затем обобщение до полного Inbox и полного AI Production Assistant. См. соответствующие документы архитектуры.

**Exit-критерий**: 1-3 пилотные компании ведут в системе реальные остатки и заказы минимум 4 недели подряд без критичных расхождений (см. критерий успеха №1 в `PROJECT_VISION.md`).

### Порядок итераций Фазы 1

Каждая итерация проходит полный цикл из `docs/QUALITY_STANDARDS.md` раздел 11 (план → реализация → тесты → документация → Architecture/Code/Security/Performance Review, + UX/AI Review где применимо) прежде чем начинается следующая. **UI (`apps/web`) сознательно идёт после доменного ядра**, а не в начале — фундамент, который нельзя переделать позже без боли (данные, права, аудит), должен быть решён прежде, чем строится интерфейс поверх него.

**Пересмотр стратегии (2026-07-26), после самоаудита архитектуры по итогам Итераций 0-6.** Владелец проекта явно сменил принцип приоритизации оставшейся части Фазы 1: **не горизонтальные инфраструктурные итерации одна за другой, а вертикальные бизнес-сценарии** — что быстрее даёт реальную работающую ценность бизнесу, а не абстрактное «ещё один слой готов». Конкретное следствие: обнаруженный при самоаудите пробел (в `apps/api` не было ни одного read/list-эндпоинта, кроме health-check) не закрывается созданием read-слоя для всех 11 модулей заранее — вместо этого read-эндпоинты появляются по одному, ровно под нужды конкретного вертикального сценария, органично, а не «на вырост». По той же причине `AI Production Assistant`, `Telegram Integration` и минимальный `Document Engine` (все три — спроектированы заранее как отдельные документы, см. карту документации выше) **перестают быть Итерацией 9/Фазой 2 и становятся Итерацией 7** — узко нацеленной на один сквозной сценарий, не на полную реализацию каждой подсистемы целиком. Структурированное логирование (было Итерацией 7) не отменяется, но перестаёт быть отдельной итерацией «на будущее» — реализуется точечно, вместе с появлением первого реального асинхронного потока (Telegram-webhook, Итерация 7), когда видимость происходящего становится реальной, а не гипотетической потребностью. Резервное копирование (было Итерацией 8) поднимается по срочности — должно быть готово до подключения первой реальной компании к системе, то есть не позже Итерации 7.

| № | Итерация | Содержание | UX/AI Review применимы? |
|---|---|---|---|
| 1 ✅ | Инфраструктурный скелет | Monorepo (pnpm/Turborepo), Docker Compose (Postgres/Redis/MinIO), CI-скелет, tsconfig/eslint, `apps/api` с health-check — без бизнес-логики. Завершена: lint/typecheck/test/build проходят чисто, health-check проверен вручную (собранный `dist/main.js` отвечает на `/health`) | Нет / Нет |
| 2 ✅ | Схема базы данных | Drizzle-схема и миграции по `docs/DATABASE_SCHEMA.md` (`packages/db-schema`): 44 таблицы (включая `inbox_channels`/`inbox_items`/`inbox_suggestions`, добавленные после аудита `USER_JOURNEY_AUDIT.md`), 24 enum-типа, 89 внешних ключей по всем модулям (Identity, Catalog+Collections, Materials, BOM, Contract Manufacturing, Warehouse, Logistics/Shipments, Sales, Marketplace Integration, Honest Sign, Finance, Documents/Notes, Inbox). Завершена: миграции сгенерированы и применены к реальному Postgres (2 миграции — исходная схема + аудит-правки: draft-статусы для `production_orders`/`suppliers`/`workshops`, `production_orders.received_at`, таблицы Inbox), проверена идемпотентность, сквозной smoke-тест полного цикла (коллекция → SKU → закупка → BOM → цех → заказ → склад → документы/заметки) и сценарий «Inbox создаёт черновик закупки → подтверждение» прошли успешно; unit-тест схемы (состав таблиц + мультитенантность корневых таблиц) в CI | Нет / Нет |
| 3 ✅ | Доменная модель | `packages/domain/*` — сущности, инварианты (включая найденные в `USER_JOURNEY_AUDIT.md`: BOM должен быть approved перед `production_order`, запрет `transfer`/`dispatch` при недостатке остатка), use cases (application services) для модулей Фазы 1, без HTTP-слоя. Завершена в две партии, обе утверждены владельцем проекта без замечаний: (1) Identity & Access, Catalog, Materials & Procurement, BOM, Contract Manufacturing; (2) Warehouse & Inventory, Sales & Orders, Marketplace Integration, Honest Sign, Finance, Notifications. Итого 11 пакетов `packages/domain/*`, единый шаблон `domain/application/infrastructure/index.ts` на каждый, только один явный синхронный межмодульный порт (`BomApprovalPort`, Contract Manufacturing → BOM) — остальные модули независимы (решение по хореографии, `ARCHITECTURE_SELF_REVIEW.md` раздел 11, ждёт Domain Events). Все интеграционные тесты — на реальном Postgres, включая два сквозных бизнес-сценария («модель → BOM → закупка → заказ в цех», «склад → продажа на Wildberries → гашение кода маркировки → себестоимость/доход»), не абстрактные примеры. CI (`ci.yml`) дополнен Postgres-сервисом и явным `globalEnv` в `turbo.json` — без этого Turborepo strict env mode обрезал `DATABASE_URL` в тестах. Зафиксированы (не реализованы) 4 направления развития — см. раздел «Итерация 3 — статус и зафиксированные направления развития» выше: bulk-операции каталога, Domain Events, Read Models/CQRS, демо-база на реальных данных. Inbox use cases не потребовали отдельного `packages/domain/inbox` — Inbox (Итерация 9) будет вызывать уже готовые `create*Draft`/`confirm*` use cases напрямую | Нет / Да |
| 4 ✅ | API | `apps/api` (NestJS) — REST-контроллеры поверх доменных use cases, OpenAPI-контракт. Завершена: единый паттерн (`*.tokens.ts` с DI-токенами доменных портов → `*.module.ts` factory providers → `*.service.ts`, зависящий от интерфейса, не от Drizze → контроллеры с `nestjs-zod` DTO из `packages/shared-types`) применён ко всем 11 доменным модулям — Identity, Catalog, Materials & Procurement, BOM, Contract Manufacturing (включая `BomApprovalPort` как отдельный provider), Warehouse & Inventory, Sales & Orders, Marketplace Integration, Honest Sign, Finance, Notifications. Владелец проекта утвердил паттерн после ответа на 5 вопросов (мультитенантность — честно не enforced на API-слое, отложено на Итерацию 5; транзакции — 1 метод репозитория = 1 агрегат, уже корректно; Domain Events/Outbox — слот уже есть в существующих транзакциях, апgrade без переписывания API; Repository Factory — не нужна отдельная абстракция, использован DI apps/api через токены; API-versioning — `/v1` через `app.enableVersioning`) — оба точечных усиления (versioning, DI-токены) внедрены в Identity перед тиражированием на остальные 10 модулей без дополнительных остановок. Каждый модуль проверен e2e-тестом на реальном Postgres (используя API уже реализованных модулей для сидинга, не прямые INSERT), суммарно 12 test-файлов/20 тестов в `apps/api`, полный pipeline (build/lint/typecheck/test) зелёный по всем 27 задачам монорепозитория | Нет / Да |
| 5 ✅ | Роли и права (RBAC) | JWT access (15 мин) + opaque refresh (30 дней, ротация с обнаружением повторного использования — reuse отзывает всю "семью" токенов), 7 предустановленных ролей (owner/director/accountant/procurement_manager/marketplace_manager/warehouse_keeper/viewer) как данные (`roles`/`permissions`/`role_permissions`, не хардкод — компания может переопределить роль тем же кодом), 23 permissions по 11 модулям + `bom.approve`, глобальные `JwtAuthGuard`/`PermissionsGuard` (fail-closed, `@Public()` как явное исключение), `@CurrentUser()` вместо client-supplied `companyId` во всех 11 модулях, онбординг компании только через CLI-бутстрап (`bootstrap-company.script.ts`) — `CompaniesController` удалён. `notifications` — единственное исключение из ролевой матрицы: доступ по владению записью (`userId`), не по роли. Завершена: все 11 модулей (Identity, Catalog, Materials & Procurement, BOM, Contract Manufacturing, Warehouse & Inventory, Sales & Orders, Marketplace Integration, Honest Sign, Finance, Notifications) защищены `@RequirePermissions(...)` по утверждённой матрице (`docs/AUTH_ARCHITECTURE.md`, раздел 6), e2e-тесты используют настоящий JWT через `/v1/auth/login` (не обход guard'ов) и проверяют 401/403 для каждого модуля. Полный monorepo pipeline (build/lint/typecheck/test, 14 пакетов) зелёный; дополнительно вручную проверен запуск собранного сервера: bootstrap → login → 401 без токена → 201 с токеном | Нет / Да |
| 6 ✅ | Механизм аудита | `packages/domain/audit` — `recordAuditEntry` use case поверх уже существующей таблицы `audit_log` (создана в Итерации 2). Добавлены `source` (`http_api`/`cli`/`telegram`/`ai` — единый журнал независимо от того, кто инициировал действие, docs/AUTH_ARCHITECTURE.md раздел 13) и nullable `inbox_suggestion_id → inbox_suggestions.id` (для будущего AI Production Assistant — какое именно предложение AI подтвердил человек). `apps/api/src/audit/AuditService` — глобальный провайдер, доступный всем модулям без явного импорта. Встроено в явно названные в `docs/ARCHITECTURE.md` раздел 7 критичные операции: warehouse (receive/dispatch/transfer — реальное изменение остатка, before/after quantityOnHand), honest-sign (issue/apply/introduce/retire — до этого `marking_code_events` фиксировал переход, но не то, кто из пользователей его выполнил), finance (проводки-транзакции и все переходы статуса счёта), identity (создание пользователя — и через HTTP `identity.write`, и через CLI-бутстрап, оба пути пишут в один журнал с разным `source`). Сознательно НЕ покрыто этой итерацией (расширение до полного покрытия, не архитектурное решение): резервирование/снятие резерва склада, инвентаризация, создание записей каталога/закупок/BOM/производственных заказов — не отнесены к явно названным в архитектуре критичным операциям. HTTP-эндпоинт чтения audit_log сознательно не добавлен — потребовал бы расширения утверждённой матрицы permissions (docs/AUTH_ARCHITECTURE.md) новым модулем, что является архитектурным решением, а не частью этой итерации. Полный monorepo pipeline (build/lint/typecheck/test, 15 пакетов) зелёный; e2e-тесты проверяют реальные строки audit_log (source/before/after) для всех 4 инструментированных модулей; вручную проверены оба пути (`cli` через bootstrap-company.script.ts, `http_api` через login+запрос) на собранном сервере | Нет / Нет |
| 7 ✅ | **Вертикальный сценарий 1: Production Order → Спецификация → PDF → Telegram → Цех → Статус** | Первый работающий сквозной бизнес-процесс целиком — не горизонтальная инфраструктурная подсистема (решение владельца проекта 2026-07-26). Целевой результат: пользователь пишет боту размеченный текст запроса — через минуту получает готовую спецификацию в PDF, отправленную (после подтверждения) в цех. Реализовано как узкий минимум каждой из четырёх заранее спроектированных подсистем: **(1)** `apps/api/src/ai-production-assistant` — `AIClassifier` (адаптер: `RuleBasedAIClassifier` без `ANTHROPIC_API_KEY` понимает строгий размеченный формат «Модель: … Цвета: … Размеры: … Цена пошива — …»; `AnthropicAIClassifier` — реальный адаптер, включается по наличию ключа) + детерминированное распределение объёма по размерам (`size-distribution.ts`, не LLM — финансово значимое число); **(2)** `ProductionOrderOrchestrationService` резолвит модель/approved BOM/SKU из уже существующего каталога (не придумывает недостающее — явная ошибка `PRODUCT_NOT_FOUND`/`BOM_NOT_FOUND`/`PRODUCT_VARIANT_NOT_FOUND`) и вызывает существующий `createProductionOrderDraft`; расчёт потребности в материалах и черновик закупки недостающего — **не реализованы в этой итерации** (сознательно сужено против первоначального плана — см. ниже); **(3)** **Document Template Engine** — `packages/domain/document` полностью переработан сверх исходного плана по прямому требованию владельца проекта (2026-07-26, приложен эталонный образец): `SpecificationTemplateDefinition` (данные, не код) + `PdfLibTemplateRenderer` (кириллица через встроенный DejaVu Sans, `@pdf-lib/fontkit`) + `document_derivatives` (type=`structured_data`) хранит шаблон+версию+данные для one-click `regenerateSpecificationDocument` (Immutable Original); **(4)** `apps/api/src/telegram` — один бот (`LoggingTelegramClient`/`HttpTelegramClient` по наличию `TELEGRAM_BOT_TOKEN`), инвайт-код для привязки цеха, исходящий PDF через `sendDocument`, входящий текстовый ответ цеха → `updateProductionOrderStatusFromWorkshop` (простой набор ключевых слов "готов"/"в работе", не NLU). Read-эндпоинты добавлены точечно под сценарий (не read-слой для всех модулей): `GET /v1/products?name=`, `GET /v1/products/:id`, `GET /v1/production-orders/:id`, `GET /v1/documents?entityType=&entityId=`. Сквозной e2e-тест (`production-order-orchestration.e2e.spec.ts`) проходит весь путь на реальном Postgres: текст → `parse` → `create-order` → `confirm` → `generate-specification` → привязка цеха в Telegram (`/start`) → входящий "Готово" → статус `ready_for_pickup`. `LocalFileStorageAdapter` — fallback `StorageAdapter` для окружений без MinIO (та же DI-фабрика, что `LoggingTelegramClient`); `S3StorageAdapter` — реальный путь при настроенном `S3_ENDPOINT`. **Сознательно не реализовано в этой итерации** (зафиксировано как явное сужение, не пробел): расчёт потребности в материалах по BOM + сверка со складом + черновик закупки недостающего (`packages/domain/procurement`); `workshopId` передаётся явно в запросе на создание заказа, не резолвится AI из текста (в примерах текст не называет цех); реквизиты договора (номер/дата/условия оплаты) — в спецификации остаются пустыми, поскольку договоры как сущность пока не смоделированы в БД (не изобретаются). Требует от владельца проекта: регистрация Telegram-бота через @BotFather и токен, `ANTHROPIC_API_KEY` — для перехода с `RuleBasedAIClassifier` на реальное распознавание свободного текста (оба не может быть выполнено автономно). **Дополнено 2026-07-27** (владелец проекта обнаружил реальный пробел: входящие сообщения компании в Telegram только логировались как `inbox_item`, ничего не создавали) — добавлен полный разговорный цикл: текст → предпросмотр ("что понял/что нашёл/чего не хватает/какие проблемы" — не просто Да/Нет) → явное подтверждение → создание заказа + подтверждение + генерация спецификации + отправка цеху одним действием. Закреплён архитектурный принцип: **Telegram — тонкий интерфейс, вся бизнес-логика в GarmentOS** (`ProductionOrderOrchestrationService`), чтобы тот же сценарий позже заработал через Web/мобильное приложение/WhatsApp без изменений в логике. Шрифт спецификации заменён на Liberation Serif (метрически как Times New Roman, эталон владельца); нумерация спецификаций — атомарный счётчик на цех (`workshops.nextSpecificationNumber`), договор цеха (`contractNumber`/`contractDate`) — реальные поля, не изобретаются | Да / Да |
| 8 | Резервное копирование | Поднято по срочности — должно быть готово **до** подключения первой реальной компании к системе. Бэкапы Postgres/MinIO на офсайт-хранилище (`docs/INFRASTRUCTURE.md`, раздел 3) + восстановление проверено на практике. **Аудит 2026-07-27 подтвердил: не начато — только диаграмма в документации, ни одного скрипта/cron в `infra/`** | Нет / Нет |
| 9 | Завершить Telegram Owner Flow | **Пересмотрено 2026-07-27** — по итогам MVP-аудита (`docs/MVP_CHECKLIST.md`) вместо перехода к новым модулям сначала закрываются 2 реальных разрыва в уже построенном сценарии Итерации 7: **(1)** компания не получает уведомление, когда цех прислал статус — `handleWorkshopReply` отвечает только цеху, не пишет в чат компании; **(2)** свободный текст запроса (без строгих меток "Модель: ... Цена пошива —") не распознаётся без `ANTHROPIC_API_KEY` — требует решения владельца проекта (подключить ключ или закрепить строгий формат как рабочий стандарт) | Да / Да |
| 10 | Полный цикл производства | **Пересмотрено 2026-07-27.** Замыкает сценарий до конца силами уже существующей схемы, без нового bounded context (Итерация 15 ниже — решение отдельным документом): приёмка партии на склад, привязанная к `production_order` (сегодня `receiveStock` физически не связан с заказом, `receivedAt`/статус `received` нигде не устанавливаются), и закрытие заказа через существующий статус `received` как терминальный (без брака/партий — это сознательно отложено в Итерацию 15). Дополнительно: свериться с реальным образцом спецификации цеха — показан владельцу проекта 2026-07-27 на модели "Худи Петроль", решить какие из отсутствующих полей (артикул, состав ткани, расход материалов, фурнитура, требования к пошиву, упаковка, QR/ID партии) добавить в шаблон **до**, а не после встраивания его во все процессы | Да / Да |
| 11 | `apps/web` — Mobile-First | Первый веб-интерфейс. **Обязательное условие, зафиксированное владельцем проекта 2026-07-27 как принцип (`docs/PRINCIPLES.md`, принцип 20), не как пожелание к дизайну**: полностью пригоден для работы с телефона в браузере, ни одной операции "только на компьютере", камера — полноценный канал ввода (фото ткани/брака/накладных, скан QR/штрихкода), не более 2-3 нажатий до типового действия, Telegram deep-link открывает конкретный экран. Формы/таблицы для того, что Telegram объективно не покрывает: настройки, объёмные отчёты, массовое редактирование | Да / Нет |
| 12 | Dashboard и аналитика | Витрины read-model'ей поверх уже накопленных за Итерации 7-10 данных (не новые таблицы) — статус заказов, документы, себестоимость | Нет / Нет |

**Итерации 13+ — после первого рабочего запуска (Version 2), зафиксировано владельцем проекта 2026-07-27**: не начинаются, пока не пройден чек-лист `docs/MVP_CHECKLIST.md` и система не используется в реальной работе минимум неделю — идеи для доработки после этого должны идти от реального использования, не от заранее придуманного списка.

| № | Итерация | Содержание |
|---|---|---|
| 13 | Universal Inbox (обобщение сценария 1) | Расширение узкого парсера Итерации 7 до полной AI-классификации всех форматов и каналов (фото/PDF/Excel/пересланные сообщения и т.д., `INBOX_ARCHITECTURE.md`) |
| 14 | AI Production Assistant (полный) | Остальные сценарии `AI_PRODUCTION_ASSISTANT_ARCHITECTURE.md`, не покрытые узким Итерацией 7: генерация спецификации без готового BOM, полноценное согласование через Workflow Engine, версии/e-signature в Document Engine, полный двусторонний обмен с цехом |
| 15 | Баланс производственной партии | `docs/PRODUCTION_BATCH_LIFECYCLE_ARCHITECTURE.md` — архитектура предложена 2026-07-27, ждёт утверждения владельцем проекта; явно приостановлено до завершения MVP («не хочу добавлять новые крупные бизнес-модули», 2026-07-27) |
| 16 | Wildberries-коннектор | `packages/connectors/wildberries` — первая реализация `MarketplaceConnector` (`docs/ARCHITECTURE.md` п.5.1) |

Технический долг, осознанно отложенный на любой итерации, фиксируется явным пунктом в начале следующей — не теряется молча (см. «Принцип ведения роадмапа» ниже).

### Итерация 3 — статус и зафиксированные направления развития (2026-07-25)

**Первая партия домена утверждена владельцем проекта** (Identity & Access, Catalog, Materials & Procurement, BOM, Contract Manufacturing) — архитектура (модульная структура `domain/application/infrastructure/index.ts`, границы агрегатов, порты для межмодульных вызовов вроде `BomApprovalPort`, draft-first через `*Draft`+`confirm*`) прошла ревью и не пересматривается для оставшихся модулей второй партии (Warehouse & Inventory, Sales & Orders, Marketplace Integration, Honest Sign, Finance, Notifications) — они реализуются по тому же шаблону.

При ревью первой партии владелец проекта явно попросил зафиксировать здесь (не реализовывать сейчас) 4 направления развития:

1. **Bulk-операции для каталога.** Массовая генерация SKU (размеры × цвета × другие параметры одной операцией) — будущий **отдельный use case** в `packages/domain/catalog` (например, `bulkCreateProductVariants`), не изменение существующих `createProduct`/`createProductVariant` и не изменение границ агрегатов `Product`/`ProductVariant`. Реализуется, когда появится конкретный сценарий использования (Zero Input, `PRINCIPLES.md` принцип 17) — вероятно, вместе с `apps/web` (Итерация 11) или как часть Inbox-сценария «прислал прайс на 12 цветов сразу».
2. **Domain Events.** После завершения доменного слоя (обе партии Итерации 3) — архитектура доменных событий (`BomApproved`, `PurchaseOrderReceived`, `ProductionOrderConfirmed`, `ShipmentCreated` и т.п.), см. `docs/ARCHITECTURE.md` п.3 («Правило межмодульного взаимодействия», in-process event bus). **Не реализуется сейчас** — текущие межмодульные вызовы (Contract Manufacturing → BOM) идут через узкие порты (`BomApprovalPort`), не через шину событий; это осознанно, чтобы не проектировать шину до того, как появится второй-третий реальный сценарий подписки на событие. Кандидат на реализацию — при переходе к сквозному сценарию продажи (`docs/ARCHITECTURE.md` п.4.1: Sales → Warehouse → Honest Sign → Finance), который в этой партии реализуется как независимые модули без хореографии между ними (см. ниже) — это и есть первый реальный повод внедрить события, не раньше. Закрывает как решение риски 2.2/2.3 из `ARCHITECTURE_SELF_REVIEW.md`.
3. **Read Models / CQRS.** Карточки сущностей (Entity Timeline, `PRINCIPLES.md` принцип 18 п.5), аналитика/Reporting BI, история документов, дашборды — вероятно потребуют специализированные модели чтения отдельно от доменных use case. **Не реализуется сейчас** — зафиксировано как возможное направление, без изменения существующих use case. Естественное место реализации — Итерация 11 (`apps/web`) и Фаза 3 (BI-дашборды).
4. **Демо-база на реальных данных бизнеса.** После завершения всех доменных модулей Итерации 3 — полноценная демо-база (seed) на основе реальных моделей/поставщиков/тканей/цехов/документов владельца, для сквозной проверки всей системы. Интеграционные тесты продолжают использовать реальные бизнес-сценарии (как `scenario.spec.ts` в Contract Manufacturing), не абстрактные примеры — этот принцип не меняется и во второй партии.

**Явное архитектурное решение по хореографии между Sales/Warehouse/Honest Sign/Finance** (вторая партия): каждый из этих модулей во второй партии Итерации 3 реализуется **независимо**, с собственными use case и инвариантами, БЕЗ синхронных кросс-модульных вызовов между собой (в отличие от Contract Manufacturing → BOM, где связь была единственной и явно требовалась инвариантом). Сценарий "заказ на маркетплейсе → резерв остатка → гашение кода маркировки → проводка себестоимости" (`docs/ARCHITECTURE.md` п.4.1) остаётся описанным, но не сшитым между модулями до появления Domain Events (пункт 2 выше) — сшивать 4 модуля синхронными портами одним махом означало бы либо преждевременную связанность, либо предвосхищение шины событий без реального повода. `Warehouse` тем не менее реализует `reserveStock`/`releaseReservation` (поля `quantity_reserved` уже есть в схеме) — это готовит почву для будущей интеграции, не будучи самой интеграцией.


## Фаза 2 — Мультиканальность и комплаенс

**Цель**: расширить продажи на другие каналы и закрыть комплаенс полностью.

- ~~AI Production Assistant~~ — **перенесён в Фазу 1 (Итерации 7/14) решением владельца проекта 2026-07-26**: «для GarmentOS это один из основных модулей», не отложенная на потом идея Фазы 2. История: зафиксирован здесь 2026-07-25 как отдельный стратегический модуль с полной архитектурой (`docs/AI_PRODUCTION_ASSISTANT_ARCHITECTURE.md`); на следующий день, после самоаудита архитектуры по итогам Итераций 0-6, признано, что ждать генерализованного Inbox (было — предпосылка для старта) не нужно — первый узкий сквозной сценарий (Итерация 7) реализуется раньше и генерализуется постфактум (Итерация 14, после первого рабочего запуска MVP — уточнено 2026-07-27), а не наоборот. Детали сценариев и обязательное ограничение «AI никогда не имеет собственных прав/не подписывает документы сам» — в самом документе, не дублируются здесь.
- Интеграция Ozon и Яндекс Маркет через тот же `MarketplaceConnector` интерфейс.
- Опт и розница (собственная витрина/точки продаж) как каналы.
- Полный цикл «Честного Знака»: вывод из оборота при продаже на маркетплейсе (через оператора ЭДО), обработка возвратов/пересортицы кодов.
- Финансовый модуль: управленческий P&L по модели/каналу/партии, экспорт в 1С.
- Уведомления: низкий остаток материалов, срыв сроков производства, рассинхронизация с маркетплейсом.
- Inbox: дополнительные каналы — email-алиас, WhatsApp Business API (требует коммерческого approval, дольше Telegram) — см. `docs/INBOX_ARCHITECTURE.md`, раздел 4.

**Exit-критерий**: 0 блокировок продаж из-за ошибок маркировки у пользователей системы (критерий успеха №4); директор получает P&L без участия бухгалтера (критерий №2).

## Фаза 3 — Планирование заказов у подрядчиков и аналитика

**Цель**: перейти от учёта к планированию и прогнозированию.

- Планирование распределения заказов между независимыми подрядными цехами: сроки, загрузка каждого цеха, рейтинг надёжности/качества по цеху — **не планирование собственных производственных мощностей**, которых у нас нет.
- BI-дашборды: маржинальность, оборачиваемость, ABC-анализ по SKU.
- Базовый прогноз спроса на основе истории продаж и сезонности.
- Рекомендации по точке перезаказа материалов на основе прогноза (а не только статичного `reorder_point`).

**Exit-критерий**: точность прогноза спроса измерена и используется минимум одним пилотным клиентом для планирования закупок.

## Фаза 4 — Масштабирование продукта (SaaS)

**Цель**: превратить внутренний продукт для пилотов в самостоятельный SaaS.

- Self-service онбординг новых компаний (регистрация, настройка без участия разработчиков).
- Полноценная мультитенантность в проде (если Фазы 1-3 использовали упрощённую модель для пилотов).
- Мобильные приложения: склад (сканирование штрихкодов/QR, инвентаризация), менеджер по производству (офлайн-доступ к статусам заказов у цехов).
- Партнёрские интеграции: логистика (СДЭК, Boxberry), банки/эквайринг, ЭДО.
- Публичный API и документация для интеграторов.

**Exit-критерий**: 100+ активных компаний-клиентов (цель 3 лет из `PROJECT_VISION.md`), self-service онбординг без участия команды разработки для типового клиента.

## Фаза 5 — Отраслевая зрелость

**Цель**: соответствует горизонту 5 лет из `PROJECT_VISION.md`.

- ML-модели: прогноз спроса следующего поколения, оптимизация точки перезаказа, рекомендации по ценообразованию на маркетплейсах.
- Расширение экосистемы интеграций.
- Открытая партнёрская программа.

## Принцип ведения роадмапа

- Каждая фаза начинается только после закрытия exit-критериев предыдущей — это фиксируется явно (не «примерно готово», а конкретная проверка).
- Изменение содержания фазы — через обновление этого файла в отдельном коммите/PR с обоснованием, не молчаливым дрейфом скоупа.
- Технический долг, отложенный сознательно ради скорости, фиксируется как явный пункт в начале следующей фазы, а не теряется.

# PILOT_BUGS.md — единственный журнал проблем на время Dogfooding

> Запрошено владельцем проекта явно (2026-08-04): «Никаких GitHub Issues. Никаких Backlog. Во время пилота только один журнал.» С этого момента и до завершения Pilot v1 (критерий — раздел `docs/MASTER_BACKLOG.md`, 0.5, «Критерий завершения») это **единственное** место, куда попадает любая проблема, найденная при реальной работе с системой. Не `docs/MASTER_BACKLOG.md`, не GitHub Issues, не устные заметки. `docs/MASTER_BACKLOG.md` возобновляет приём новых пунктов только после того, как этот журнал будет разобран целиком (см. `CLAUDE.md`, правило 10).

## Зачем отдельно от `docs/PILOT_LOG.md`

Разделение ролей, чтобы не дублировать одно и то же в двух местах:

- **`docs/PILOT_LOG.md`** — свободный дневник вех: деплой прошёл, компания создана, партия перешла в следующий статус, что-то заняло много времени. Не структурирован, не про баги конкретно.
- **`PILOT_BUGS.md` (этот файл)** — только проблемы, найденные при работе с системой на реальной партии, в строгом табличном формате, с меткой критичности и статусом исправления. Это рабочий список для приоритизации, не рассказ.

## Правило «никакого Excel»

Дословно (владелец проекта, 2026-08-04): *«Нельзя пользоваться Excel, если ту же операцию можно выполнить в GarmentOS. Даже если в GarmentOS пока неудобнее.»* Если операция в системе неудобна — это не повод открыть Excel, это повод завести строку в этом журнале с меткой 🟡 или 🟢. Обход через Excel скрывает именно те проблемы, ради обнаружения которых существует Pilot v1.

## Метки критичности

Каждая проблема получает ровно одну метку:

| Метка | Значение |
|---|---|
| 🔴 | Блокирует проведение партии — без исправления следующий шаг реального процесса невозможен. |
| 🟡 | Замедляет работу — партию провести можно, но через обходной путь/руками/дольше, чем должно быть. |
| 🟢 | Просто неудобно — не мешает и не замедляет, но раздражает или могло бы быть лучше. |

**Исправление — всегда сверху вниз**: сначала все открытые 🔴, затем 🟡, затем 🟢. Не браться за 🟢, пока есть открытые 🔴 — даже если 🟢 быстрее исправить.

## Формат строки

| Дата | Партия | Что делали | Что ожидали | Что произошло | Критичность | Как исправили | Статус |
|---|---|---|---|---|---|---|---|
| _(пример, не реальная запись)_ 2026-08-05 | Партия «Худи Base» / Цех №1 | Подтверждали заказ пошива на 100 шт | Заказ перейдёт в «Размещён», сформируется Snapshot | Ошибка 500 — у цеха не был указан номер договора | 🔴 | Добавили номер договора в карточку цеха, заказ подтверждён | Закрыто |

## Журнал

| Дата | Партия | Что делали | Что ожидали | Что произошло | Критичность | Как исправили | Статус |
|---|---|---|---|---|---|---|---|
| 2026-09-11 | — (найдено при аудите tenant isolation, не при работе с реальной партией) | Читали код `GET /products/:id/sizes` (`apps/api/src/catalog/products.controller.ts` → `catalog.service.ts:listProductSizes` → `productSizes.listByProduct(productId)`) при READ-ONLY проверке API после Step 4A.2 | Эндпоинт должен резолвить размерный ряд модели только внутри своей компании — как уже сделано для `/products/:id`, `/products/:id/sizes` (PUT), вариантов и т.д. | Метод делает `SELECT ... FROM product_sizes WHERE product_id = $1` без join на `products.company_id` и без проверки принадлежности — тот же паттерн IDOR, что был закрыт в `526dd3d` для `product_variants`/`inventory_counts`/marketplace-сущностей, но этот конкретный (read-only) эндпоинт тогда пропустили. Практическое следствие: зная/подобрав чужой `productId`, любой пользователь любой компании с правом `catalog.read` может прочитать размерный ряд (`size`/`sortOrder`/`ratioWeight`) чужой модели | 🟢 | Не исправлено — на пилоте одна компания, эксплуатационного риска нет; исправление отложено до конца Pilot v1 (Freeze архитектуры, `CLAUDE.md` правило 10) | Открыто |
| 2026-09-21 | — (найдено при UX-аудите «посмотри на всё как команда разработчиков», не при работе с реальной партией) | Изучали `WorkshopsPage.tsx` — вкладки фильтра «Черновик»/«Архив» над списком цехов всегда показывали пустой список | Вкладка либо показывает реальные записи в этом статусе, либо не существует | `create-workshop.ts` всегда создаёт цех со `status: 'active'` (`draft` зарезервирован под ещё не реализованный сценарий Inbox, `archived` не проставляется нигде — заархивировать цех неоткуда), и `GET /workshops` (`workshops.controller.ts:55`) и так возвращает только активные. Вкладки были не багом фильтрации, а UI для состояний, которые сегодня физически недостижимы | 🟢 | Убраны вкладки «Черновик»/«Архив» и сам фильтр по статусу целиком (`WorkshopsPage.tsx`) — показывать нечего, пока нет ни одного способа получить цех в этих статусах; вернуть фильтр вместе с реализацией архивации после пилота | Закрыто |
| 2026-09-21 | — (найдено при том же UX-аудите, не при работе с реальной партией) | Проверяли весь интерфейс на наличие удаления/архивации ошибочно созданных записей (модель, вариант/SKU, документ) | Ошибочно созданную запись можно скрыть/удалить/заархивировать без обращения к БД напрямую | Во всём приложении нет ни одного действия «удалить» или «архивировать» для модели, варианта или документа — единственное существующее удаление в системе вообще — это удаление одной строки атрибута у модели. Если создали не ту модель или загрузили не тот документ, откатить это через интерфейс нельзя | 🟢 | Не исправлено — за один пилотный цикл (2-3 модели, 2 цеха) отсутствие отката некритично, обход есть (просто не пользоваться ошибочной записью); нужен отдельный дизайн (soft-delete vs hard-delete, что происходит со связанными заказами/остатками) — не делается по ходу пилота (Freeze архитектуры, `CLAUDE.md` правило 10) | Открыто |
| 2026-09-21 | — (найдено при сравнении с «МойСклад», не при работе с реальной партией) | Владелец проекта сравнил интерфейс с разделом «Деньги»/«Взаиморасчёты» МойСклад | В системе можно отметить оплату (предоплату/окончательный расчёт) цеху и увидеть баланс взаиморасчётов по цеху | В спецификации уже считается и печатается в PDF «Предоплата 70% составляет N руб.», но нигде не сохраняется факт оплаты. Backend для счетов/платежей существует (`invoices.controller.ts`, `transactions.controller.ts`), но только `POST`-эндпоинты (создать/провести), без единого `GET`-списка и без веб-страницы вообще (`apps/web/src/layout/AppShell.tsx`, комментарий: «Финансы» из группы «Учёт» по-прежнему не переносятся). Владелец должен отслеживать оплату цеху вне системы | 🟡 | Не исправлено по прямому решению владельца («по финансам пока не делай») — минимальный экран (список счетов/платежей + баланс по цеху) отложен до конца Pilot v1; на время пилота оплата отслеживается вне приложения | Открыто |
| 2026-09-21 | — (найдено при том же сравнении с «МойСклад») | Проверяли, можно ли распечатать штрихкод/этикетку на изделие | На складе можно распечатать этикетку с артикулом/штрихкодом для физической маркировки пачки | У каждого SKU есть поле `barcode` в БД (`product_variants.barcode`), но оно нигде не отображается и не печатается — ни на одной странице веба | 🟢 | Не исправлено по прямому решению владельца («2 пункт не нужен пока что») — отложено до конца Pilot v1 | Открыто |

_(записи о находках из реальной работы с партией начинаются после деплоя и создания первой настоящей партии)_

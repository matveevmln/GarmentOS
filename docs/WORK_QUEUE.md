# Очередь GarmentOS — Штаб партии v1

Перед T00 прочитать `docs/GOS-PARTY-V1-REVIEW.md`: это саморевью концепции 1.2, не выполненный T00 и не разрешение на код. В аудите проверить R01–R12 на соответствие существующей реализации.

Контракт интерфейса и действий: `docs/GOS-PARTY-V1-SCREEN-CONTRACT.md` (версия 1.2). Уточняет пакеты T05a–T05c и T06; эти задачи остаются PLANNED, не READY. Визуальный пакет не является работающим прототипом.

Источник требований: `docs/GOS-PARTY-V1.md`. Правила исполнения: `docs/tasks/README.md`.
Один исполнитель, одна активная задача. Эта очередь не является автоматическим планировщиком.

**Дополнение 1 к аудиту T00 (2026-09-24)**: по запросу владельца `docs/GOS-PARTY-V1-AUDIT.md` расширен — сопоставление A01–E02 и R01–R12 с кодом (разделы 16–17), многомодельный заказ с общим штабом (раздел 13), отгрузки `shipments` (раздел 14), проверка цепочки компенсации брака (раздел 15), скорректированное задание T01 (раздел 18), перечень исправленных выводов (раздел 19) и непроверенного (раздел 20). Поручение развивать GOS-PARTY-V1 вместо прежнего запрета на новые функции зафиксировано в `CLAUDE.md`, правило 10 (без разрешения на деплой, рабочие миграции и удаление данных). Статус T00 остаётся `REVIEW`, T01 остаётся `BLOCKED` до принятия T00. Вопрос из примечания ниже о расширении аудита под оба документа закрыт этим дополнением.

**Примечание к статусу T00 (обновлено 2026-09-23, второй раз)**: и пакет со скрин-контрактом, и пакет с критическим ревью 1.2 приходят с версией этой строки, вернувшей T00 в `READY` — оба готовились без учёта того, что T00 уже выполнен и находится на ревью владельца (`docs/GOS-PARTY-V1-AUDIT.md`, PR https://github.com/matveevmln/GarmentOS/pull/5). Статус ниже по-прежнему сознательно оставлен `REVIEW`, не откачен — существующая, уже отправленная на проверку работа не перезаписывается молча повторно. И `SCREEN-CONTRACT.md`, и `GOS-PARTY-V1-REVIEW.md` появились уже после отправки аудита на ревью, поэтому T00 пока НЕ проверяет соответствие ни действий A01–E02, ни правил R01–R12 коду — это по-прежнему открытый вопрос к владельцу (расширять существующий аудит под оба документа или считать его закрытым и переходить к T01, используя оба документа как дополнительный вход).

| ID | Статус | Задание | Результат |
|---|---|---|---|
| SEC-P1 | REVIEW | `docs/tasks/SEC-P1.md` — отдельное исправление изоляции компаний; Claude Sonnet | Ветка `fix/gos-sec-p1-tenant-isolation` от `gos-party-v1/review-security-handoff` (SHA `fe8df8acd76cf20201c0871c8ba54f6f30fb3131`); отчёт `docs/reports/SEC-P1.md`, PR см. описание ветки |
| T00 | REVIEW | `docs/GOS-PARTY-V1-T00-CLAUDE.md` и проверка возможностей ниже | `docs/GOS-PARTY-V1-AUDIT.md` |
| T01 | BLOCKED | Модель и заказ с сохранением; конкретное задание после аудита | Не начинать до ревью T00 |
| T02–T05 | PLANNED | См. основной документ | Не начинать по одному названию |

## Следующее действие — SEC-P1 выполнено, ждёт ревью владельца

По поручению владельца продолжить реализацию 2026-09-24 было выдано самостоятельное задание SEC-P1. Оно исправляет подтверждённый дефект существующего поведения и выполнялось до принятия T00; это исключение относится только к SEC-P1. **Реализация завершена, статус — `REVIEW`** (не `ACCEPTED` — это делает владелец). Полный отчёт: `docs/reports/SEC-P1.md` (карта входов, изменения по каждому файлу, красный/зелёный прогон regression-теста, команды и результаты typecheck/lint/build/test, ограничения). Постоянный тест: `apps/api/src/warehouse/sec-p1-tenant-isolation.e2e.spec.ts`. T00 не возвращён в READY, не объявлен ACCEPTED. T01 остаётся BLOCKED; уточнения для его ADR — в `docs/GOS-PARTY-V1-TRANSITION-REVIEW.md`.

## Перед T00: проверка возможностей Claude (история задания)

Модель: Opus. Прочитай инструкции репозитория и оба документа GOS-PARTY-V1.
1. Подтверди репозиторий, ветку и SHA исходного кода. Не путай доступ GitHub MCP с доступом к терминалу.
2. Укажи доступность чтения/записи GitHub, локального выполнения команд, браузера и отдельной тестовой БД. Не раскрывай секреты и не выводи окружение целиком.
3. Если терминал есть: запиши версии Node/pnpm и выполни одну подходящую безопасную проверку из package.json; зафиксируй команду, код выхода, результат. Сначала изучи скрипт. Не запускай seed, миграции и тесты против production.
4. При отсутствии терминала продолжай статический аудит по GitHub; проверки выполнения явно пометь недоступными. Не выдумывай результаты.
5. Изменяй только документацию в отдельной ветке. Не коммить в main или ветку Railway. На текущем этапе не писать функциональный код, не менять зависимости и CI.
6. Выполни T00. Сохрани отчёт и обнови статус T00 на REVIEW (или BLOCKED с точной причиной). Создай черновик PR при доступных правах; не сливай его.
7. В конце дай ссылку на PR/ветку, SHA и коротко перечисли проверенное и непроверенное. Остановись до ревью; не объявляй задачу ACCEPTED самостоятельно.

Аудит может выполняться на базе ветки пакета заданий; укажи её исходный commit и отличие от актуальной рабочей ветки. При конфликте не затирай изменения других участников.

## Статусы

READY → IN_PROGRESS → REVIEW → ACCEPTED. BLOCKED содержит причину и доступную продолженную работу.
Только после принятия результата ревьюером следующая задача получает READY и конкретное задание.
Нет фонового исполнителя и автоматического ревью: эти механизмы ещё не настроены.

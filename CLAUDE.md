# CLAUDE.md — главный документ проекта GarmentOS

> Этот файл — точка входа для любого разработчика (человека или AI-агента), заходящего в репозиторий. Он не дублирует остальные документы, а **указывает, куда идти** и фиксирует правила, обязательные для соблюдения при работе с кодовой базой.

## 0. Статус репозитория

**Фаза 0 — Фундамент закрыта (2026-07-24).** Архитектура согласована владельцем проекта, все открытые вопросы из `docs/ARCHITECTURE_SELF_REVIEW.md` закрыты явными решениями. **Текущая стадия: Фаза 1 — реализация MVP**, см. `docs/ROADMAP.md`.

Окончательные решения, зафиксированные на Фазе 0 (не пересматриваются без нового ADR):
- Инфраструктура cloud-agnostic, «Start Lean, Scale Smart» (`docs/INFRASTRUCTURE.md`).
- Мультитенантность: единая БД, изоляция по `company_id` (`docs/ARCHITECTURE.md` п.6).
- Первый маркетплейс: Wildberries, через `MarketplaceConnector` (`docs/ARCHITECTURE.md` п.5.1).
- Local-First: собственная БД — единственный источник истины (`docs/PRINCIPLES.md` принцип 14).
- AI-First: **уточнено 2026-07-24** — AI-классификация (модуль `Inbox`) реализуется уже в Фазе 1, не откладывается; правило «AI предлагает, домен исполняет, человек подтверждает» остаётся неизменным (`docs/PRINCIPLES.md` принцип 15).
- **Бизнес-модель уточнена перед Итерацией 2** (`docs/DATABASE_SCHEMA.md` п.0): GarmentOS обслуживает бренд/селлера, который размещает пошив в независимых подрядных цехах — не собственное производство. Это привело к аудиту и правке схемы БД (удалены `tech_specs`/`production_stages`, добавлена `workshops`) и терминологии — см. `PROJECT_VISION.md` раздел 1.
- **Zero Input — главный критерий приоритизации, зафиксирован перед Итерацией 3** (`docs/PRINCIPLES.md` принцип 17): каждая новая сущность/правило/use case проверяется вопросом «что пользователь сможет делать быстрее благодаря этому». По итогам дня-в-жизни-владельца (`docs/USER_JOURNEY_AUDIT.md`) в Фазу 1 добавлен модуль `Inbox` (`docs/INBOX_ARCHITECTURE.md`) — универсальная точка входа (Telegram/файлы/голос), где AI распознаёт и предлагает готовое действие, человек подтверждает одним нажатием.
- **Архитектура Universal Inbox утверждена владельцем проекта (2026-07-24)**, включая принцип 18 (полная связанность документов, `document_links`) и принцип 19 (Immutable Original — оригинал документа неизменяем, версии — цепочка строк `supersedes_document_id`, производные данные AI — отдельная таблица `document_derivatives`; см. `docs/ARCHITECTURE_SELF_REVIEW.md`, раздел 10). Итерация 3 (доменный слой) открыта.

**Правило для любого агента/разработчика на Фазе 1:**
1. Разработка идёт маленькими итерациями строго по порядку из `docs/ROADMAP.md` («Порядок итераций Фазы 1») — сначала данные и домен (БД → доменная модель → API → RBAC → аудит → логирование → бэкапы), затем Inbox+Telegram-бот (итерация 9) и Wildberries (итерация 10), общий веб-UI с формами — итерация 11 (не раньше).
2. Ни одна итерация не считается завершённой без прохождения ревью-гейта (`docs/QUALITY_STANDARDS.md`, раздел 11): план → реализация → тесты → документация → Architecture/Code/Security/Performance Review (+ UX/AI Review где применимо, обязательна проверка по критериям Zero Input).
3. Перед началом первой итерации с кодом — свежая проверка архитектуры на критические риски (если появились новые после последних решений).
4. Если тебя просят «добавить фичу» вне текущей итерации по роадмапу — сначала уточни, не преждевременно ли это.
5. При проектировании любого use case — не только «правильно ли смоделировано», но и «сколько кликов и ручного ввода это требует» (`docs/PRINCIPLES.md`, принцип 17).

## 1. Карта документации

| Документ | Назначение |
|---|---|
| [`PROJECT_VISION.md`](./PROJECT_VISION.md) | Миссия, цели 1/3/5 лет, пользователи, критерии успеха, границы MVP |
| [`README.md`](./README.md) | Публичное лицо проекта: что это, как запустить, для кого |
| [`docs/ARCHITECTURE.md`](./docs/ARCHITECTURE.md) | Полная системная архитектура: модули, слои, диаграммы, интеграции |
| [`docs/TECH_STACK.md`](./docs/TECH_STACK.md) | Технологический стек и обоснование каждого выбора |
| [`docs/REPOSITORY_STRUCTURE.md`](./docs/REPOSITORY_STRUCTURE.md) | Структура монорепозитория, назначение каждой директории |
| [`docs/DATABASE_SCHEMA.md`](./docs/DATABASE_SCHEMA.md) | Структура базы данных, ER-диаграмма, конвенции |
| [`docs/INFRASTRUCTURE.md`](./docs/INFRASTRUCTURE.md) | Cloud-agnostic инфраструктура: Start Lean Scale Smart, эталонный стек, поэтапный план роста |
| [`docs/USER_JOURNEY_AUDIT.md`](./docs/USER_JOURNEY_AUDIT.md) | Аудит дня владельца бизнеса в системе (11 шагов) — источник найденных пробелов и приоритетов |
| [`docs/INBOX_ARCHITECTURE.md`](./docs/INBOX_ARCHITECTURE.md) | Universal Inbox — AI-классификация входящих сообщений, Zero Input |
| [`docs/AUTH_ARCHITECTURE.md`](./docs/AUTH_ARCHITECTURE.md) | Аутентификация и авторизация (Итерация 5): JWT, refresh-токены, роли, permissions, мультитенантность на уровне API |
| [`docs/AI_PRODUCTION_ASSISTANT_ARCHITECTURE.md`](./docs/AI_PRODUCTION_ASSISTANT_ARCHITECTURE.md) | AI Production Assistant (основной модуль Фазы 1, не Фаза 2 — уточнено 2026-07-26) — расчёт спецификаций/материалов из текста/голоса, генерация документов, согласование, обмен с цехом через Telegram |
| [`docs/TELEGRAM_INTEGRATION_ARCHITECTURE.md`](./docs/TELEGRAM_INTEGRATION_ARCHITECTURE.md) | Telegram-бот как транспорт: один бот на всю систему, входящий/исходящий поток, привязка компаний и внешних участников (цех/поставщик) |
| [`docs/DOCUMENT_ENGINE_ARCHITECTURE.md`](./docs/DOCUMENT_ENGINE_ARCHITECTURE.md) | Document Engine — владение `documents`/`document_links`/`document_derivatives`, генерация PDF, подготовка к электронной подписи |
| [`docs/WORKFLOW_ENGINE_ARCHITECTURE.md`](./docs/WORKFLOW_ENGINE_ARCHITECTURE.md) | Workflow Engine — узкоспециализированный механизм многошагового согласования документов (не общий BPMN-движок) |
| [`docs/MASTER_BACKLOG.md`](./docs/MASTER_BACKLOG.md) | Единый план развития поперёк итераций (заведён 2026-08-02 по итогам UX-аудита прототипа) — все находки/техдолг/идеи новых модулей попадают только сюда; `ROADMAP.md` остаётся источником истины по фазам и порядку, этот документ — по приоритету и обоснованию |
| [`docs/PRODUCTION_BATCH_LIFECYCLE_ARCHITECTURE.md`](./docs/PRODUCTION_BATCH_LIFECYCLE_ARCHITECTURE.md) | Баланс производственной партии — полный жизненный цикл партии от заказа до закрытия (крой → пошив → ОТК → отправка → приёмка → брак → устранение: дошив/перешив/скидка/списание → финансовая сверка), предотвращение повторной оплаты, причины и место выявления брака, обязательства цеха, KPI цехов (предложено 2026-07-27, ждёт утверждения) |
| [`docs/ROADMAP.md`](./docs/ROADMAP.md) | Дорожная карта разработки по фазам |
| [`docs/PRINCIPLES.md`](./docs/PRINCIPLES.md) | Принципы разработки (инженерные и продуктовые) |
| [`docs/QUALITY_STANDARDS.md`](./docs/QUALITY_STANDARDS.md) | Стандарты качества кода, тестирования, CI/CD |
| [`docs/ARCHITECTURE_SELF_REVIEW.md`](./docs/ARCHITECTURE_SELF_REVIEW.md) | Самопроверка архитектуры, риски, открытые вопросы |
| [`docs/ARCHITECTURE_REVIEW.md`](./docs/ARCHITECTURE_REVIEW.md) | Независимая архитектурная ревизия перед первым реальным запуском на Railway (2026-08-02) — сверка `docs/*` с фактическим кодом/схемой БД, приоритеты P0-P3 |
| [`docs/WEB_DESIGN_SYSTEM.md`](./docs/WEB_DESIGN_SYSTEM.md) | Эталонный визуальный язык GarmentOS (утверждённый `apps/prototype`) — аудит соответствия `apps/web`, план переноса компонент за компонентом |
| [`docs/UI_FOUNDATION.md`](./docs/UI_FOUNDATION.md) | Инженерный фундамент UI Kit — какие open-source библиотеки (shadcn/ui, Radix UI, Tailwind, TanStack и др.) лежат в основе фирменных Garment-компонентов, какие компоненты строятся сейчас, какие зарезервированы на будущее |
| [`docs/DESIGN_SYSTEM_MAP.md`](./docs/DESIGN_SYSTEM_MAP.md) | Полная карта компонентов Design System (расширяет `UI_FOUNDATION.md`) — исследование лучших SaaS/библиотек, полная таблица компонентов по категориям с Tier A/B-now/B-later, самостоятельный UX-аудит, benchmark |
| [`docs/UX_PRINCIPLES.md`](./docs/UX_PRINCIPLES.md) | Операционные UX-правила поверх принципов 17/20/22 из `PRINCIPLES.md` — одинаковое поведение элементов, пустые/ошибочные состояния, Undo, автосохранение, accessibility-минимум, каталог `/design-system` |
| [`docs/UI_REVIEW.md`](./docs/UI_REVIEW.md) | Приёмочный обзор дизайн-системы для владельца проекта — что реализовано, что изменилось по сравнению со старым интерфейсом, что отложено, известные компромиссы, результаты Playwright-проверки |
| `docs/adr/` | Architecture Decision Records — по одному файлу на значимое решение (создаётся по мере необходимости) |

## 2. Что такое GarmentOS (кратко)

Операционная система для **бренда одежды и селлера** (не производителя): закупка материалов → спецификации (BOM) → размещение и контроль заказов пошива в независимых подрядных цехах → склад (SKU по размеру/цвету) → продажи (маркетплейсы) → маркировка «Честный Знак» → финансы. Мы не шьём сами — см. уточнение бизнес-модели в `PROJECT_VISION.md` (раздел 1) и `docs/DATABASE_SCHEMA.md` (п.0).

## 3. Ключевые архитектурные решения (сводка)

Полный текст — `docs/ARCHITECTURE.md`. Здесь — то, что должен знать любой, кто пишет код:

- **Стиль**: модульный монолит с чёткими границами модулей (domain modules), готовый к выделению отдельных сервисов без переписывания домена.
- **API-first**: любой модуль сначала описывается контрактом (OpenAPI), потом реализуется.
- **Многотенантность закладывается с первого дня** на уровне схемы БД (`company_id` в доменных таблицах), даже если MVP разворачивается как single-tenant.
- **Интеграции с маркетплейсами** — через единый интерфейс адаптера (`MarketplaceConnector`), а не встраиванием специфики WB/Ozon в доменную логику.
- **Честный Знак — отдельный bounded context**, не размазан по другим модулям.
- **Инфраструктура cloud-agnostic** (принцип «Start Lean, Scale Smart»): Docker, PostgreSQL, Redis, S3-совместимое хранилище за адаптером — переносимо между self-hosted (включая Кыргызстан по стоимости), Yandex Cloud, VK Cloud, AWS, GCP, Azure без изменений в коде. Подробности — `docs/INFRASTRUCTURE.md`.
- Технологический стек — TypeScript везде (backend + frontend), PostgreSQL, Redis, монорепозиторий на pnpm + Turborepo. Обоснование — `docs/TECH_STACK.md`.

## 4. Терминологический глоссарий (используй именно эти термины в коде и документах)

Последовательность терминологии критична для читаемости кода в предметной области, где много синонимов.

| Русский термин | Английский эквивалент в коде | Пояснение |
|---|---|---|
| Коллекция | `collection` | Сезонная группа моделей (например, «Осень 2026») |
| Модель (изделия) | `product` | Абстрактная модель одежды (например, «Худи Base»), принадлежит `collection` |
| Артикул / SKU | `sku` / `productVariant` | Конкретное сочетание модель × размер × цвет |
| Спецификация | `bom` (bill of materials) | Нормы расхода материалов на единицу модели |
| Швейный цех (подрядчик) | `workshop` | Независимая компания, выполняющая пошив по нашему заказу — **не наш сотрудник и не наш цех** |
| Заказ (пошива) | `productionOrder` | Заказ на пошив определённого объёма модели у конкретного `workshop` |
| Остаток | `stock` / `stockItem` | Количество SKU на складе (включая склад-локацию у цеха — см. `docs/DATABASE_SCHEMA.md` п.8-9) |
| Остаток материала | `materialStockItem` | Количество материала (ткань/фурнитура) на складе — отдельно от `stockItem` (тот про готовые SKU, не сырьё); добавлено 2026-08-02 для проверки наличия материалов перед созданием заказа пошива |
| Приёмка | `goodsReceipt` | Поступление товара/материала на склад (`stock_movements.type = 'receipt'`) |
| Исход со склад покупателю | `dispatch` | Окончательное выбытие товара из компании при продаже (`stock_movements.type = 'dispatch'`) — не путать с `shipment` ниже |
| Отгрузка / экспорт (между складами) | `shipment` | Перемещение между **нашими** складами (например, страна пошива → страна продаж); физическое движение остатков — `stock_movements.type = 'transfer'` |
| Документ | `document` | Файл (инвойс, договор, накладная, фото, сертификат) — сам по себе без привязки к сущности. Оригинал неизменяем (`docs/PRINCIPLES.md` принцип 19); новая редакция — новая строка через `supersedesDocumentId` |
| Связь документа | `documentLink` | Многие-ко-многим: один документ может относиться сразу к модели, материалу, поставщику и закупке одновременно (`docs/PRINCIPLES.md` принцип 18) |
| Производные данные документа | `documentDerivative` | OCR/перевод/структурные данные/AI-саммари, извлечённые из документа — отдельная запись, ссылающаяся на неизменяемый оригинал, не замена ему (`docs/PRINCIPLES.md` принцип 19) |
| Заметка | `note` | Свободный текстовый комментарий к одной конкретной сущности (не файл, не автоматический аудит-лог) |
| Входящее сообщение | `inboxItem` | Сырое сообщение/фото/голос/файл из Telegram и др. каналов, ещё не превращённое в доменную запись |
| Предложение (Inbox) | `inboxSuggestion` | Результат AI-классификации `inboxItem`: какой use case вызвать, с какими предзаполненными полями — исполняется только после подтверждения человеком |
| Честный Знак | `honestSign` / `chestnyZnak` | Система обязательной маркировки товаров в РФ (ГИС МТ) |
| Код маркировки | `markingCode` | Уникальный DataMatrix-код единицы товара |
| Маркетплейс | `marketplace` | Внешняя торговая площадка (WB, Ozon, ЯМ) |
| Личный кабинет / кабинет продавца | `marketplaceAccount` | Учётная запись компании на конкретном маркетплейсе |
| Компания / тенант | `company` / `tenant` | Юрлицо-владелец данных в системе (мультитенантность) |

Не изобретай синонимы (`item` вместо `sku`, `warehouse_balance` вместо `stock`) — сверяйся с этой таблицей и расширяй её при появлении новых понятий.

## 5. Конвенции разработки (когда появится код)

- **Язык кода**: TypeScript strict mode везде. Комментарии и коммиты — на русском или английском, но единообразно внутри модуля (предпочтительно английский для идентификаторов, русский — для бизнес-документации).
- **Коммиты**: [Conventional Commits](https://www.conventionalcommits.org/) (`feat:`, `fix:`, `docs:`, `refactor:`, `test:`, `chore:`). Пример: `feat(warehouse): добавить инвентаризацию по SKU`.
- **Ветки**: `main` — защищённая, `feature/<module>-<short-desc>`, `fix/<short-desc>`. Никаких долгоживущих feature-веток дольше одного спринта (см. `docs/PRINCIPLES.md`, trunk-based подход).
- **PR**: не мержится без прохождения CI (lint, typecheck, test) и минимум одного ревью.
- Подробные стандарты — `docs/QUALITY_STANDARDS.md`.

## 6. Правила для AI-агентов, работающих в этом репозитории

1. Перед реализацией любой фичи — свериться с `docs/ARCHITECTURE.md` и `docs/DATABASE_SCHEMA.md`, не изобретать параллельную структуру.
2. Если задача противоречит `PROJECT_VISION.md` (например, просят фичу вне скоупа MVP) — явно сообщить об этом пользователю, а не молча реализовывать.
3. Значимые архитектурные решения, не описанные в текущих доках — оформлять как ADR в `docs/adr/NNNN-title.md`, не просто менять код.
4. Не добавлять новые языки/фреймворки/базы данных без обновления `docs/TECH_STACK.md` и явного согласия владельца проекта.
5. Изменения в терминологии — только через обновление глоссария в этом файле.
6. Данный файл (`CLAUDE.md`) обновляется при любом значимом изменении архитектуры — он должен оставаться точным входным документом, а не устаревшим артефактом.
7. **Язык документации — русский (зафиксировано 2026-07-25).** Вся документация, комментарии в коде, ADR, README, архитектурные описания, отчёты о проделанной работе, названия задач и пояснения для владельца проекта — на русском языке. Исключения: код; названия классов/методов/переменных/таблиц БД; API, протоколы и общепринятые технические термины, которые традиционно остаются на английском. Новые документы сразу пишутся на русском; существующие англоязычные документы переводятся постепенно, при изменении соответствующих разделов — без отдельной массовой миграции.
8. **Уровень автономности — Senior/Staff Engineer.** В рамках уже утверждённой архитектуры (документы из раздела 1) технические решения (выбор конкретных алгоритмов/библиотек в рамках `docs/TECH_STACK.md`, структура кода, разбиение на use case, конкретика реализации) принимаются самостоятельно, без остановки на подтверждение. Владелец проекта привлекается только при (а) необходимости изменить уже принятое архитектурное решение или (б) решении, кодирующем бизнес-логику/бизнес-правило (например, кто из ролей что видит — матрица прав доступа, правила онбординга компании и т.п.) — такие места явно помечаются в документах (например, `docs/AUTH_ARCHITECTURE.md`, раздел 0) и не реализуются в коде до явного подтверждения.
9. **Инициатива по улучшениям (зафиксировано 2026-08-02, `docs/MASTER_BACKLOG.md` раздел 9).** Если в процессе работы обнаружено более правильное архитектурное решение, повторяющаяся ручная операция, потенциальная ошибка бизнес-процесса или возможность автоматизации — не молчать до отдельного задания. Сначала предложить изменение (обоснование, ожидаемый эффект, влияние на архитектуру) владельцу проекта; после согласования — занести в `docs/MASTER_BACKLOG.md`; реализовывать только после этого. Разовые находки не превращаются в код в обход этого пути.
